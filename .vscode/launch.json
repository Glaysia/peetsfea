{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run current file from run/",
      "type": "debugpy",
      "request": "launch",
      "python": "${workspaceFolder}/.venv/bin/python3",
      "program": "${file}",
      "cwd": "${workspaceFolder}/run",
      "env": {
        "PEETSFEA_DEBUG": "1"
      },
      "preLaunchTask": "prepare-sample-build-debug",
      "console": "integratedTerminal"
    }
  ]
}
