# 0.2.22 Type2 PyAEDT STEP Import

## Goal
- 완료된 non-model baseline artifact를 headless HFSS 세션으로 import하는 smoke path를 둔다.
- PyAEDT `0.25.1`의 `Modeler3D.import_3d_cad()` 계약을 repository fail-fast wrapper 안에 고정한다.
- runtime manifest/build dispatch에는 아직 연결하지 않고 AEDT import 가능성만 빠르게 검증한다.
- 이 계획은 [[sdd/plans/0.2.22-type2-step-to-em-validate-pipeline]]의 2번 마일스톤인 non-model headless import smoke 단계다.
- 이 smoke path는 historical non-model artifact 기준이다. 이후 modeled/non-model 통합 import는 [[sdd/plans/0.2.22-type2-toml-unification]]의 `examples/type2.toml`에서 생성된 artifacts를 입력으로 삼는다.

## Scope
- 포함:
  - [[sdd/code/entry/import_non_model_step_to_hfss.py]]
  - [[sdd/code/src/peetsfea/aedt/wrappers.py]]
  - [[sdd/code/src/peetsfea/aedt/protocols.py]]
  - [[sdd/code/tests/backend_em/test_type2_step_import_smoke.py]]
- 제외:
  - `entry/build.py`, `entry/sample.py`, manifest runtime dispatch 변경
  - modeled coil import, EM-ready object assembly, EM pipeline 실행
  - GUI validation
  - PyAEDT stable docs의 newer `input_file_unit` 인자 사용

## Decisions
- smoke script는 `Hfss(project=None, design=..., non_graphical=True, new_desktop=True)`로 headless 세션을 만든다.
- import 전후 `modeler.object_names` 차이를 새 imported object ledger로 삼고, 빈 diff는 fail-fast 대상이다.
- imported object는 모두 `set_object_model_state(name, False)`로 AEDT non-model guide object가 된다.
- `.aedt` 출력은 `run/aedt/type2_step_import_smoke/type2_non_model_objects_import.aedt` 아래에 저장한다.
- wrapper는 PyAEDT `0.25.1` 로컬 시그니처만 노출하고 `input_file_unit` fallback/대체 경로를 추가하지 않는다.
- 이 단계는 현재 non-model artifact의 import 가능성과 wrapper fail-fast 계약만 확인하며, modeled object registry나 `EmPipelineInput` 조립까지 확장하지 않는다.
- type2 unified import 단계에서는 `type2.toml`에서 파생된 metadata ledger와 import diff를 합치는 adapter를 별도 구현한다.

## Affected Notes
- 관련 완료 baseline: [[sdd/plans/0.2.22-type2-build123d-non-model-step]]
- 관련 전체 계획: [[sdd/plans/0.2.22-type2-step-to-em-validate-pipeline]]
- 관련 TOML 단일화 계획: [[sdd/plans/0.2.22-type2-toml-unification]]
- 관련 첫 modeled prototype: [[sdd/plans/tx-rect-void-step-generator]]
- 관련 코드 노트:
  - [[sdd/code/entry/import_non_model_step_to_hfss.py]]
  - [[sdd/code/src/peetsfea/aedt/wrappers.py]]
  - [[sdd/code/src/peetsfea/aedt/protocols.py]]
  - [[sdd/code/tests/backend_em/test_type2_step_import_smoke.py]]
- 관련 type2 아키텍처 노트: [[sdd/architecture/type2-step-to-em-validate-pipeline]]

## Acceptance
- pure-Python tests cover false-return `import_3d_cad`, invalid `object_names`, and the fake-HFSS smoke path.
- `.venv/bin/python -m pytest tests/backend_em/test_type2_step_import_smoke.py` succeeds without launching AEDT.
- `.venv/bin/pyright` succeeds for touched `src/` and `tests/` contracts.
- Real AEDT validation remains opt-in: run from `run/` with `../.venv/bin/python ../entry/import_non_model_step_to_hfss.py`.
