---
title: 0.2.25 Type2 Quarter-Turn Single Coil
created: 2026-05-21 @ 00:00
updated: 2026-05-21 @ 00:00
tags:
  - plan
  - type2
  - single-coil
  - sampling
---

# 0.2.25 Type2 Quarter-Turn Single Coil

## Goal
- Replace single-coil integer `turn_count`/fixed `terminal_path` ownership with sampled quarter-turn ownership.
- Use per-coil sampled owners `x_ratio`, `y_ratio`, `turn_qcount`, `void_factor`, `metal_fill_factor`, `terminal_start`, and `void_stack_present`.
- Derive clockwise terminal metadata internally from `terminal_start` and `turn_qcount`; TOML no longer owns `terminal_path`.

## Contract
- `turn_qcount` is an integer quarter-turn count. Effective turn count is `turn_qcount / 4.0`.
- Realized metadata keeps `effective_turn_count = turn_qcount / 4.0`, while geometry clearance sizing may use a larger routing-depth count when the quarter-turn path reaches a deeper ring.
- `terminal_start` is an integer owner with candidates `0..3`, mapped to `A/B/C/D`.
- Winding direction is fixed to `cw`; the derived end corner is `(terminal_start + turn_qcount) % 4`.
- `turn_qcount=4n` must preserve the prior same-corner `n`-turn geometry.
- All quarter-turn counts use one centerline pipeline: generate the axis-aligned quarter-turn skeleton, then apply blunt-corner shaping wherever the path has an internal corner. A one-segment `turn_qcount=1` path naturally remains straight.
- Quarter-turn counts whose partial lap reaches a deeper ring, such as `turn_qcount=7`, must size trace/gap pitch from routing depth rather than from `turn_qcount / 4.0` alone so copper does not enter the centered void keepout.
- RX gains `void_stack_present` with the same semantic switch as TX inner void stack presence.
- Legacy single-coil TOML keys `outer_x_usage_ratio`, `outer_y_usage_ratio`, `turn_count`, `void_usage_ratio`, and `terminal_path` are unsupported in the new active type2 examples.

## Dimension Audit
- Active sampled-owner count is 15.
- The 15 owners are `non_model_objects.tx_region.z_gap_from_rx_plane_mm` plus TX inner and RX single-coil `x_ratio`, `y_ratio`, `turn_qcount`, `void_factor`, `metal_fill_factor`, `terminal_start`, and `void_stack_present`.
- Fixed guide/reference owners and fixed `modeled_objects.tv_aluminum_plate.sheet_present` remain outside sampled metadata and build design-variable handoff.

## Work Slots
- Worker A owns core single-coil types, realization, and centerline geometry.
- Worker B owns type2 modeled spec parsing/types/resolvers and public schema validation.
- Worker C owns sampling, sampled TOML, build design-variable handoff, and example TOMLs.
- Worker D owns TX/RX void stack scene generation and ledger/body metadata for RX parity.
- Worker E owns type2 parser/export/build tests and geometry regression coverage.
- Worker F owns docs and SDD code-note synchronization, plus final dimension audit documentation.

## 2026-05-22 Quarter-Turn Centerline Unification Distribution
- Controller owns this SDD handoff, final integration review, and conflict resolution. Worker spawning follows [sub-agent-spawn-policy](../structure/sub-agent-spawn-policy.md): four `gpt-5.3-codex-spark` `medium` workers with `fork_context=false`; only failed spawn slots may fall back to `gpt-5.5` `low`.
- Worker A owns centerline unification in `src/peetsfea/tx_rect_void_centerline.py` and focused centerline tests. It removes the `turn_qcount < 4` smoothing skip, keeps one skeleton-to-blunt pipeline, and preserves q=4/q=8 full-turn seed contracts.
- Worker B owns routing-depth realization in `src/peetsfea/tx_rect_void_spec.py` and core geometry regressions. It keeps metadata `effective_turn_count = turn_qcount / 4.0` while sizing pitch/trace/gap from `max(effective_turn_count, routing_depth)`.
- Worker C owns Type2 STEP/export regression coverage in `tests/type2/test_generate_type2_step.py`. It updates partial-turn expectations away from axis-aligned-only geometry and adds fixed-like q=7 TX inner void-stack export coverage.
- Worker D owns reusable core test/document synchronization for `tests/tx_rect_void/test_tx_rect_void.py` and matching code notes. It adds q=1/q=2/q=3/q=5/q=7 behavior assertions without changing example TOML or staged files.

## Verification
- Targeted pure Python type2 tests must pass for spec tools, sampling, build entry, setup STEP entry, and STEP generation geometry coverage.
- Pyright must be clean for changed Python files.
- Active sampled owner audit must report `tx_region.z_gap_from_rx_plane_mm` plus seven sampled owners per coil.
